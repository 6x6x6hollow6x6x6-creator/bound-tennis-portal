# 引き継ぎメモ

Claude（Cowork）で作ったものを Claude Code に引き継ぐための記録。
設計の約束事は `CLAUDE.md` に書いてあるので、先にそちらを読むこと。

---

## この案件の経緯

協会から「全国各地のローカル大会情報のまとめサイトがほしい」「いろんな大会を含めた日本ランキングを作りたい
（硬式テニスのJOPみたいな感じ）」という相談があり、たたき台を作っている段階。

デザインの方向性は何度か往復した結果、**日本バウンドテニス協会サイト（boundtennis.or.jp）の配色・レイアウト準拠**で確定。
実際にブラウザで開いて色を拾った：ネイビー `#001F33`、レッド `#B50000`、背景 `#F1F1F1`、罫線 `#E2E2E2`、本文 `#222`。
書体も協会サイトと同じ `Helvetica Neue → Hiragino Kaku Gothic ProN` の指定にしてある。

アーキテクチャは、依頼者が以前 Claude Code で作った**石川県バウンドテニス協会サイト（ibta.info）と同じ構成**を
踏襲するよう指定された。単一 `index.html` ＋ GitHub Pages ＋ Apps Script ＋ Googleドライブ。

---

## 動作確認済みのこと

Playwrightで通しテスト済み。

- サンプルデータでの全画面表示（トップ／大会情報／ランキング／大会結果／お知らせ／制度）
- 大会の絞り込み7種類、月別グルーピング、ページング
- ランキングの区分切り替え（15区分）、内訳の展開、選手の戦績モーダル
- 管理画面：大会追加 → 結果登録 → 承認 → ランキング反映 → 保存 → リロードして復元
- 配点変更の再計算（`topN` を 6→1 にすると 444pt→306pt に変わることを確認）
- XSS：お知らせのタイトルに `<script>` を入れても実行されない。`javascript:` のURLは `href` に出ない
- レスポンシブ：1360 / 820 / 390px で横スクロール発生なし

### 実CDNでの表示確認（2026-08-17 実施・完了）

`cdn.tailwindcss.com` と `unpkg.com/lucide` に実際に取りに行って、1360 / 820 / 390px の
全画面と管理画面の全タブをスクリーンショットで目視確認した。Tailwindのカスタムカラーが
効いていること（`bg-navy` → `rgb(0,31,51)`、`text-brand-red` → `rgb(181,0,0)`）も計算スタイルで確認。
JSエラー・読み込み失敗ともになし。

見つかった崩れは以下4件。すべて修正済み（`index.html`）。

| 症状 | 原因 | 対処 |
|---|---|---|
| 制度ページのグレード表・配点表が390pxで1文字ずつ縦に折り返す | グレードのチップが `nowrap` で幅を占め、本文列が潰れていた | 表を `overflow-x-auto` で包み、`min-w-[560px]` を付けて横スクロールさせる |
| 768〜1023pxでヘッダーのメニュー項目が2行に割れる | 6項目が入りきらないのに `md:`（768px）で横並びに切り替えていた | 切り替えを `lg:`（1024px）に変更。820pxはハンバーガーになる |
| 管理画面の「承認待ち」バッジが2行に割れる（PC幅でも） | 状態列が `w-20` でバッジより狭かった | `w-24` に広げ、バッジを `inline-block` + `whitespace-nowrap` に |
| ランキングの見出し・チップ、選手一覧の所属が狭い幅で1文字ずつ折り返す | 短い語に `whitespace-nowrap` が無かった | 該当箇所に `whitespace-nowrap`。チップの親には `flex-wrap` を足して溢れを防ぐ |

再確認は `node tools/smoke-test.mjs`（実CDN・全項目パス）。

### Apps Script との疎通（2026-08-18 実施・GETのみ完了）

スプレッドシートと Apps Script を作り、ウェブアプリとしてデプロイ済み。
`SHEETS_API_URL` は `index.html` に記入済み。

- 5種類すべての GET が `{"ok":true,"items":[...]}` を返すことを確認
  （`tournaments` / `players` / `results` / `news` / `content`）
- 不正な type には `{"ok":false,"error":"unknown-type"}` を返すことを確認
- ブラウザから実際に読めることを確認（`DATA_SOURCE === 'api'`）。
  CORS は問題なし。ただし `file://` からは弾かれるのでローカル確認はサーバー経由で

> **いまサイトは空です。** スプレッドシートにまだ1件もデータが無いため、
> 大会0件・選手0件で表示されます。3段フォールバックの設計どおり、APIが応答している以上
> `DEFAULT_DATA` のサンプルには戻りません（配点表とサイト文言だけは `DEFAULT_DATA` で補完される）。
> サンプル表示に戻したいときは `SHEETS_API_URL` を一時的に空にする。

`tools/smoke-test.mjs` は画面の回帰テストなので、既定で Apps Script への通信を遮断して
`DEFAULT_DATA` で走るようにした。本番シートの中身に結果が左右されないようにするため。
APIとの疎通込みで見たいときは `USE_API=1 node tools/smoke-test.mjs`。

### サインイン（2026-08-18 時点・生成元の反映待ちで中断）

クライアントID `576378291172-vukiiep7ohj6t7e59be58ioh09pi87gp.apps.googleusercontent.com` を
`index.html` と `Code.gs` の両方に記入済み。

動くようになったところ：

- `CONFIGURED()` が true になり、フッターの「お試しモード」の入口が消えた
- Googleサインインのボタンが描画される

止まっているところ：

- `[GSI_LOGGER]: The given origin is not allowed for the given client ID.` で
  サインインが実行できない（`http://localhost:8000` で確認）

切り分け済み。**クライアントIDの設定ミスではない。**

- Googleの認可エンドポイントに問い合わせると `redirect_uri_mismatch` が返る
  → クライアントは実在し、ウェブアプリケーション型として正しく登録されている
  （`invalid_client` なら存在しない、の意味になる）
- Cloud Console 側では `http://localhost:8000` を JavaScript 生成元に登録済み（本人確認）
- したがって残るは**Google側の反映待ち**。公式に5分〜数時間かかるとされている

localhost の登録は開発時の便宜にすぎないので、**公開先ドメインが決まったらそちらを登録して
本番で検証するほうが早い**。localhost が通るのを待つ必要はない。

確認用スクリプトを `t.mjs`（gitignore済み）に置いてある。`node t.mjs` で再確認できる。

## 未確認のこと

- **POST（書き込み）。** サインインが通らないと試せないので未検証
- **`file://` ではサインインは動かない**（生成元として登録できないため）。
  ローカルで試すときは必ずローカルサーバー経由で、かつ生成元に登録したポートで

---

## 残タスク

### すぐやること

1. ~~**`Code.gs` のプレースホルダを埋める**~~ ✅ 完了（2026-08-18）
   - `ALLOWED_EMAILS` … `6x6x6hollow6x6x6@gmail.com`
   - `GOOGLE_CLIENT_ID` … 記入済み

   > ⚠️ **`Code.gs` を貼り直したら再デプロイが必要。** クライアントIDを入れた版はまだ
   > Apps Script 側に反映していない可能性がある。「デプロイを管理 → ✏️ → バージョン=新バージョン」。
   > 「新しいデプロイ」を選ぶとURLが変わってしまうので注意
2. ~~**`index.html` の値を埋める**~~ ✅ 完了（2026-08-18）

   **置き場所（2026-08-17 決定）**：スプレッドシートは個人アカウント
   `6x6x6hollow6x6x6@gmail.com` のドライブ「アプリ用」フォルダに作る。
   シートの所有者がそのまま Apps Script を動かすアカウントになるので、
   `ALLOWED_EMAILS` と同じアカウントで揃っていることになる。

   > ⚠️ Claude に接続されているドライブは協会アカウント `ishikawaboundtennis@gmail.com` のほう。
   > 個人アカウント側は見えないので、Claude にドライブ操作を頼んでも「アプリ用」は見つからない。
3. ~~**実CDNでレイアウト確認**~~ ✅ 完了（上の「実CDNでの表示確認」を参照）
4. ~~**GitHubリポジトリを作って Pages で公開**~~ ✅ 完了（2026-08-18）

   - リポジトリ： https://github.com/6x6x6hollow6x6x6-creator/bound-tennis-portal （**Public**）
   - サイト： https://6x6x6hollow6x6x6-creator.github.io/bound-tennis-portal/
   - 既定ブランチは `main`（`master` からリネーム済み）。`main` の `/` を Pages が配信する
   - 公開サイトで表示・API読み込み（`DATA_SOURCE === 'api'`）・レイアウトを確認済み

5. **OAuthの生成元に本番ドメインを登録する** ⬜ ←**いまここ**

   Cloud Console → クライアント → 承認済みの JavaScript 生成元 に次を追加：

   ```
   https://6x6x6hollow6x6x6-creator.github.io
   ```

   ドメインまで。**リポジトリ名（`/bound-tennis-portal`）は付けない。末尾スラッシュも付けない。**
   これを入れるまでサインインは `The given origin is not allowed` で弾かれる。

### そのあと

5. **ロゴの差し替え。** `index.html` 内のSVGはプレースホルダー
6. **サンプルデータの入れ替え。** 管理画面で本物を入れて「JSON書き出し」→ `DEFAULT_DATA` を差し替え、
   またはスプレッドシートに直接投入
7. **注意バーを消す。** 管理画面「サイト文言」の「上部の注意バー」を空欄にすると帯が消える

### 余裕があれば

8. ~~**結果のCSV一括取込。**~~ ✅ 完了（2026-08-18。管理画面「結果」タブの「まとめて貼り付ける」）
9. **`&since=` の活用。** `Code.gs` のGETに期間絞り込みを実装済みだがフロントは未使用。
   結果が数千行になったら使う
10. **BTラリー戦の記録ランキング。** 制度ページに「別立てとする予定」と書いたまま未実装

---

---

## 制度の方針転換（2026-08-18）

依頼者から実態の説明があり、前提が違っていたことが分かった。

> 交流大会は大会ごとに内容が違い、**クラス分けがない大会が多い**。
> 全国で行われる大会の9割は地方の交流大会で、公式戦と言えるのは
> 全国大会（本戦・県予選）、ブロック選手権（本戦・県予選）、県選手権、
> 国民スポーツ大会（団体戦のみ）くらいしかない。

元の設計は「ローカル大会の成績も同じ物差しで比較できる」前提に立っていたが、
クラス分けが無ければ優勝の意味が大会ごとに違うので、その前提は成り立たない。
配点表もドロー係数も、クラス分けとブラケットがある大会でなければ機能しない。

**決定：ランキングは公式戦だけで組み、交流大会は掲載にとどめる。**

| 項目 | 変更前 | 変更後 |
|---|---|---|
| G4・G5の配点 | 90〜180pt | **すべて0**（掲載のみ） |
| 有効大会数 | 上位6大会 | **4大会**（公式戦は年に数回しかない） |
| 掲載条件 | 2大会以上の出場 | **1大会以上** |
| 対象種目 | 全種目 | **シングルス・ダブルス・ミックスダブルス**（`settings.rankEvents`） |

団体戦は掲載できるようにしたが、ランキングには算入しない。
出場していない控えの選手にも順位が付いてしまい、貢献度を測れないため。
チーム単位の団体戦ランキングを別立てにするかは未決（下の未決事項）。

実装上の要点：

- 配点0のグレードは `computeRankings` が結果ごとスキップするので、集計から完全に外れる
  （出場回数にも数えられない）。設定を戻せば元の挙動に戻る
- 制度ページの「掲載のみ」バッジと文言は、**配点から自動判定**している。
  固定テキストに書くと設定を変えたときに食い違うので、書かないこと
- 種目は2本立て。`EVENTS_ALL`（掲載できる種目・団体戦とBTラリー戦を含む）と
  `settings.rankEvents`（ランキング対象）
- 管理画面の結果登録で、対象外の種目を選ぶとポイント欄が「—」になり
  「ランキングには算入されません」と出る。数字を見せると誤解を生むため

ついでに直した既存の不具合：種目がBTラリー戦だけの大会を選ぶと、種目の選択肢が空のまま
登録でき、`ev` が空文字の結果ができてしまっていた。保存を止めるようにした。

## 地域差・ドロー数の是正（2026-08-18）

依頼者からさらに指摘があった。

> ブロックや都道府県でレベルや参加人数に偏りがある。北海道はブロック大会も北海道だけで
> くくられているので有利になる。地域によっては参加した瞬間に入賞が決まることもある。

調べたところ、係数表に穴があって実際に順位が逆転していた。

```
修正前
  G2 2名ドローで優勝（1試合勝つ）      = 300 pt
  G3 32名ドローで優勝（5試合勝つ）      = 360 pt   ← ほぼ同じ
  G2 4名ドローで初戦負け（ベスト4確定） = 135 pt
  G3 32名ドローでベスト8（3試合勝つ）   = 101 pt   ← 負けた人のほうが高い
```

原因は2つ。**係数表の最小バケットが「4名以上」しかなく、それ未満は全部0.50に落ちていた**こと
（`drawCoef` は最小バケット未満のとき最後の行の係数を返す。1名でも0.50）。
もう1つは、**成績ラベルが「何人抜いたか」の代理指標として機能していない**こと。

対処（すべて実施済み）：

1. **勝ち数0の成績は「出場」に読み替える**（`winsNeeded()` / `awardedPoint()`）。
   ドロー数と成績から必要勝ち数を計算できる（32名ドローの優勝＝5勝、4名ドローのベスト4＝0勝）。
   設定 `winBasedPlaces` を false にすると元の挙動に戻る
2. **係数表に `2名以上＝0.30` を追加**して下限を作った
3. **グレードは競う範囲で決めるルールを制度ページに明記**（`about.gradeRule`）。
   実質1県のブロック大会はG3として扱う。コード変更ではなく運用ルール

```
修正後
  G2 2名ドローで優勝                    = 180 pt
  G2 4名ドローで初戦負け                =   9 pt   ← 出場扱いになった
  G3 32名ドローでベスト8（3試合勝つ）   = 101 pt   ← 逆転が解消
```

あわせて、**ドロー数未入力（0）だと係数0.50が黙って適用されていた**問題も直した。
事務局が入力を忘れると半分のポイントが付いてしまう状態だったので、登録時に弾くようにし、
入力欄にも警告を出すようにした。

スモークテストに「勝ち数0の入賞は出場扱い」「勝った選手のほうが点が高い」を追加してある。

## レーティング化に向けた準備（2026-08-18）

依頼者との議論で出た論点。

> 北海道だけブロック大会のグレードが違う時点で公平感はない。対戦相手のレーティングでの
> 重みづけができるのが一番いいが、データ集計に数年かかる（過去の履歴を集めてもいい）。
> 東北ブロックで勝ち上がるのと関東ブロックで勝ち上がるのではかなりレベルに開きがある。

**決定：記録の粒度を上げる（全参加者を記録できるようにする）＋ ブロック較正は全国大会の実績で行う。**

### なぜ記録の粒度を先に上げるのか

**データは遡って作れない。** レーティングの実装自体は100行程度で、ボトルネックは常にデータ。
いま上位8人だけで運用すると、数年後に「レーティングをやろう」となったとき、
その数年分は使えない。逆に全参加者を記録しておけば、あとから方式を選べる。

粒度の段階：

| 段階 | 記録する内容 | 後でできること |
|---|---|---|
| 以前 | 上位8人 | 配点表による集計のみ |
| **いまここ** | **全参加者** | フィールドの強さ補正、参加実態の把握 |
| 次 | 対戦結果（誰が誰に勝ったか） | Elo / Glicko によるレーティング |

Eloは順位では計算できず、対戦単位の記録が要る。目安は1人あたり15〜25試合で安定しはじめる。
公式戦が年4〜5大会・1大会3〜5試合なら2〜3年分。過去のドロー表が残っていれば、
1大会のPDFから数十試合ぶん復元できるので、履歴収集で短縮はできる。
ただし全国・ブロック・各県に散らばっており、県協会への依頼が要る。技術ではなく調整の仕事。

**見落としやすいコスト**：レーティングは説明しにくい。「なぜ優勝したのに点が下がったのか」に
事務局が答えられる必要がある。配点表なら表を見せれば済むが、レーティングはそうはいかない。
設計方針③（事務局が手作業で回せる程度に単純）と正面から衝突する。

### 実装したもの：まとめて貼り付け

管理画面の「結果」タブ下部。1行に1名、`成績,選手IDまたは氏名`。タブ区切りも可。
列の順番が逆でも読む。氏名は空白を無視して照合する。

安全側に倒してある点：

- 名簿にない選手、不明な成績はエラーとして列挙し、**候補から外す**
- **同じ選手が複数行にいたら、その選手を丸ごと候補から外す。**
  どちらの成績が正しいか決められないため。片方を勝手に採ると誤ったポイントが入る
- 同姓同名が複数いる場合は選手IDを要求する
- 登録前に必ずプレビュー（成績・選手・所属・ポイント）を出す

### まだ実装していないもの：ブロック較正

**データが1件も無い状態で自動較正を実装しても、正しく動いているか検証できない。**
全国大会の結果が数回分たまってから作る。仕様は下記。

- 各ブロック代表が**全国大会**でどこまで勝ち上がったかを集計し、ブロック係数を出す
- 必要なデータは全国大会の結果だけ。どのみち必ず集めるもの
- 「協会がブロックの格を決めた」ではなく「全国大会の実績がこう出ている」と言えるので説明しやすい
- 精度はEloに劣るが、全ブロック一律G2よりは確実にまし
- 設定に `regionCoef`（地域ごとの係数）を足し、G2の大会に掛ける形を想定

## シード制度への対応（2026-08-18）

依頼者から競技構造の説明があった。**これが制度上いちばん効いた指摘。**

> 前年の全国大会で3位までに入ると翌年の全国大会のシード枠が与えられるため、
> 翌年の全国大会予選やブロック大会（予選・本戦とも）には参加しなくなる。
> ブロック大会本戦の優勝者には全国大会のブロックシード枠が与えられる。
> 基本的にどの県も全国大会に一人でも多く出そうとする。

つまり**強い選手ほど出場する大会が少ない**。合計方式はこれと構造的に相性が悪い。試算した結果：

```
全国大会で優勝したシード選手      1000 pt
予選から積み上げた非シード選手    1572 pt   ← 572pt 上
全国大会ベスト4のシード選手        450 pt   ← 非シードの29%
```

**日本一がランキング1位になれない状態だった。**

### 検討した案

| | 1位 | 2位 | 3位 | 4位 | 5位 |
|---|---|---|---|---|---|
| 現状（合計） | 非シード 1572 | 全国優勝 1000 | 県のみ 712 | 全国ベスト4 450 | 全国ベスト8 280 |
| 案A：平均のみ | 全国優勝 500 | 非シード 393 | 県のみ 237 | 全国ベスト4 225 | 全国ベスト8 140 |
| **案B：平均＋G1×2** | **全国優勝 1000** | **全国ベスト4 450** | **非シード 393** | **全国ベスト8 280** | **県のみ 237** |

案Aは平均にするだけでは足りず、全国ベスト4が県だけ回る選手より下に残る。
**案Bを採用。**

### 実装

1. **平均方式**（`settings.averaged` / `minDivisor`）
   BTP＝上位N大会の合計 ÷ `max(採用大会数, 2)`。
   除数を「採用大会数」にしてあるので上位4で頭打ちになり、**多く出場して損をすることはない**。
   交流大会は0点なので `computeRankings` に入らず、分母にも影響しない。
   `averaged: false` にすると合計方式に戻る
2. **G1の配点を2倍**（優勝2000／ベスト4 900）。G2との比が1.67倍→3.3倍
3. ランキングの内訳は「合計 ÷ 除数 ＝ BTP」と分解して表示。
   構成比の棒グラフは合計を基準にしている（`row.sum`）

### 依頼者が提案した別案（不採用）

「シード枠に相当ポイントを与える」案もあった。合計方式のまま説明が簡単という利点はあるが、
**架空の成績を作る**ことと、**シードで出て初戦負けした選手にもブロック優勝相当が入ってしまう**
のが難点。案Bは架空の記録なしに同じ公平性を得られるので採用しなかった。

### 全国大会のドロー表

依頼者いわく、**日本バウンドテニス協会の公式サイトから結果付きで取得できる**。
これでブロック較正の材料はすぐ揃う（各ブロック代表が全国でどこまで勝ったか）。
ドロー表があれば対戦結果も復元できるので、レーティングの素地にもなる。

## 集計期間の延長と、制度の限界の明記（2026-08-18）

依頼者から、県ごとの選手層の厚さの違いについて指摘があった。

> 神奈川など一部の県は選手層が厚いので、他県でなら優勝できるレベルでも入賞できず、
> なかなか全国大会に出られない。実力が拮抗しているので毎年全国に出る人間が変わる。
> 一方、地方では予選に参加すれば全国大会出場の切符がもらえて毎年同じ人が出る。

### これはポイント制では解けない

ポイントは「その大会で何位だったか」に与えられるので、**どの大会に出られたかで決まる**。
神奈川の3番手が県予選で消えれば点が入らない。実力があってもランキングに現れない。

配点表をどういじっても直らない。県予選のグレードを上げれば、今度は
「県予選で勝つほうが全国に出るより得」という別の歪みが出る。
**測っている対象が「実力」ではなく「所属県での相対順位」だから。**

これを解けるのはレーティングだけ。Eloは「誰に勝ったか」で動くので、
全国大会に出られなくても強い相手に勝てば上がる。
依頼者が当初から「対戦相手のレーティングでの重みづけが一番いい」と言っていたのは構造的に正しい。
いま見つかっている問題（ブロック間格差、県ごとの層の厚さ、シード制度）は
すべて同じ根から出ていて、すべてレーティングなら解ける。

**なお、全国大会の結果だけではブロック較正の材料として不十分**という点も判明した。
あれで測れるのは「各ブロックの代表がどれくらい強いか」であって「層が厚いか」ではない。
ブロック係数を出す目的には足りるが、層の厚さまで測るには県予選のドロー表が要る。

### いま入れた対処

1. **集計期間を52週→104週**、52週より古い成績は**0.5倍に減衰**（`settings.decay`）。
   全国大会が2回分入るのでブレが減る。シード選手は年1大会なので特に効く。
   `decay` は `[{weeks, coef}]` の配列で、空にすれば減衰なしに戻る
2. **制度の限界を制度ページに明記**（`about.limits`）。
   出場機会の差を補正できないこと、将来的にレーティングへの移行を検討していることを書いた。
   あとから「あの人のほうが強いのにランキングが低い」と言われたとき、
   隠していなかったことが効く

ランキングの内訳では、減衰した成績を `255（510×0.5）` のように分解して表示する。

## カテゴリ移動への対応（2026-08-18）

依頼者からの指摘。

> 年齢が上がってフリーからミドルへ、ミドルからシニアへというクラスの移動もあるし、
> 前年はシニアで出たけど次の年は人がいなかったからミドルで出るとかもある。

**実装に2つ穴があった。どちらも修正済み。**

### 穴1：フリーだけが特別扱いされていた

旧実装は `if (r.cat === 'フリー')` のときだけ上位カテゴリに算入していた。
つまり**シニアの選手がミドルに出ると、その成績はシニアのランキングに入らなかった**。
人数の都合でミドルに回ることがあるという実態と合っていない。

出場したカテゴリより対象年齢が上のカテゴリすべてに算入するよう一般化した。
フリー→ミドル・シニア、ミドル→シニア。逆は不可。

### 穴2：大会当日の年齢で判定していた

旧実装は `ageAt(p, t.date)`（大会当日の年齢）で判定していた。これだと
**誕生日でカテゴリが上がった選手が、直前までの成績を全部失う**。
60歳になった瞬間、シニアのランキングに実績ゼロで現れて最下位から始まることになる。

集計基準日の年齢（`ageAt(p, as)`）で判定するよう変更した。
集計期間は2年なので、この違いが影響するのは直近2年で境界を跨いだ選手だけ。
まさにその移行期の選手を救うための変更。

確認した挙動：

```
62歳の選手がミドルの部で優勝
  フリー → 掲載なし（逆方向には入らない）
  ミドル → 入る
  シニア → 入る          ← 旧実装では入らなかった

1年半前にフリーで優勝、いま61歳（当時59歳）
  フリー・ミドル・シニア すべてに入る   ← 旧実装ではシニアに入らなかった
```

### 出場資格の検査を追加（穴3）

依頼者に確認した結果、カテゴリの規則は次のとおり。

- フリー（16〜44歳）の選手はミドル・シニアに出られない
- ミドル（45〜59歳）の選手はシニアに出られない
- **逆はあり。** 年上の選手は下位カテゴリに出られる

出場カテゴリと種目の組み合わせは正しく動いていることを確認した。

```
30歳： 出場=フリー           → 掲載=フリー
50歳： 出場=フリー・ミドル    → 掲載=フリー・ミドル
62歳： シングルス=シニア     → シニア
       ダブルス=ミドル       → ミドル・シニア     （種目ごとに独立）
```

ただし**出場資格の検査が無く、30歳をシニアで登録してもそのまま通っていた**。
手入力なら気づくが、PDFから数千件を取り込むと名寄せの間違いがそのまま入る。
逆に言えば、これは**取り込みミスを検出する最良の手がかり**になる
（シニア男子のドロー表なのに30歳に紐づいたら、ほぼ確実に人違い）。

`catAgeIssue()` を追加し、結果登録と一括貼り付けの両方で検査するようにした。
年度内に達する年齢との差を1歳ぶん猶予してあるので、44歳→ミドル、59歳→シニアは通る。

### 生年不明への対応（穴4）

依頼者に確認したところ、**選手名簿は協会に頼めばもらえる見込みだが、たたき台の段階では出ない**。
将来もらえる前提で、いまはドロー表にある情報だけで進める。

ドロー表に載っているのは**氏名と都道府県だけ。生年は無い。**
ところが旧実装は `p.birth = Number(p.birth) || 1990` で、
**生年不明の選手を全員1990年生まれ（36歳）にしていた。**
このまま取り込むとシニアの選手が若者扱いになり、出場資格の検査に全部弾かれる。

対処：

- 生年は `null`（不明）を許す。既定値を入れない
- **出場したカテゴリから年齢の下限を推定する**（`minAgeOf` → `P_MIN_AGE`）。
  シニアに出ていれば60歳以上。これが唯一の手がかり
- 集計は `effAge()`（生年があればそれ、なければ推定下限）、表示は `ageText()`（「60歳以上」と出す）
- `ageAt()` は不明のとき `null` を返す

これで**生年が分からなくてもカテゴリ跨ぎが正しく動く**：

```
生年不明の選手が シニアで優勝 → 推定「60歳以上」
その選手が別大会で ミドルで優勝 → ミドルとシニアの両方に掲載
```

名簿をもらったら生年を埋めるだけでよく、埋めた選手から順に精度が上がる。

### 取り込みで残る課題（名寄せ）

パーサーより難しいのはこちら。

- ドロー表には生年が無いので、**同姓同名を区別できない**
- 5年の間に**都道府県が変わる**選手がいる（転勤・移住）
- 旧姓・新姓の変更もありうる

いまの `catAgeIssue()` は「カテゴリと年齢が矛盾する」場合しか検出できない。
生年が入っていない選手同士では働かないので、**名簿をもらうまでは名寄せの精度に限界がある**。

### レーティングに移る場合

レーティングは「誰に勝ったか」で計算するので、**カテゴリの区別自体が不要になる**。
選手単位で1つのレーティングを持てばよく、この問題は消える。
表示のためにカテゴリ別の一覧を出すことはできる。

## 全日本選手権の取り込み（2026-08-18）

協会サイトの公開PDFから、第43回・第44回の全日本選手権を取り込めるようにした。
集計期間が104週なので、ランキングに効くのはこの2大会。

### 情報源の役割分担

**ドロー表の幾何から順位を復元する方式は捨てた。** 試合そのものは95.6%読めたが、
優勝者が22種目中3種目しか公式と一致しなかった。線の位置は描画の都合で揺れるため、
閾値を詰めても安定しない（閾値を変えるたびに優勝者が変わった）。

| 情報 | 情報源 | 確度 |
|---|---|---|
| 出場者（氏名・所属） | ドロー表 | 高い。文字の取りこぼし0.1%（notdefのみ） |
| 上位8位の順位 | **成績結果Best8** | 協会が確定させた値そのもの |
| 1回戦突破の判別 | — | 使わない。全員「出場」で登録する |

成績結果Best8のPDFは**氏名の文字が抜ける**（堀江和喜→江和喜、佐々木健→佐々健）。
抜ける位置は先頭とは限らないので、**順序を保った部分列**として照合する。
これで22種目すべて、8件すべての順位が名簿に結びついた。

### ツール

```
node tools/draw-pdf.mjs   <PDF>          文字と座標を取り出す（土台）
node tools/draw-parse.mjs <PDF>          ドロー表から出場者と対戦を読む
node tools/best8.mjs      <PDF>          成績結果から上位8位を読む
node tools/build-data.mjs <フォルダ> <出力>  サイトに読み込むJSONを作る
```

`build-data.mjs` はフォルダ内の `index.json`（PDFの一覧）と `<回次>-best8.pdf` を見る。

### 取り込み結果

大会2 / 選手833 / 結果1733（優勝33・準優勝33・ベスト4 66・ベスト8 132・出場1469）。
男子フリーシングルスの1位は角田貴哉（山梨・第44回優勝）で、公式と一致する。

### 途中で踏んだ罠

- **欠場の誤判定。** ドロー表の「欠」を枠に結びつけるとき、範囲が広すぎて隣の枠や
  反対側の半分の「欠」まで拾い、**優勝者を欠場扱いで除外していた**。
  範囲を自分の半分に限定し、さらに「公式の成績結果に載っている選手は欠場していない」
  という当たり前の条件で上書きするようにした
- **所属の表記ゆれ。** 「(福　井)」「北信越/福井」「北海道ﾌﾞﾛｯｸ」を揃えないと、
  同じ選手が別人として二重に登録される。実際に松田慶聖が3位と4位に並んでいた

### 順位の刻みと同順位表示（2026-08-18）

依頼者から「同じポイントがずらっと並ぶのを何とかしたい」「同点は同順位で表示してほしい」。

**同点は同順位にした**（1,2,2,4…）。大会が少ないうちは同点が多く出るので、
並び順の都合で上下が付いているように見えるのを避ける。

**刻みは「1回戦突破」を1段追加できた。** 上位8位は成績結果から、それ以外は
ドロー表で1回戦を勝ったかどうかだけを見る。

なぜ1回戦だけか。**2回戦以降は使えないことを検証で確認した。**
復元した対戦から勝ち数を数えると、公式の優勝者22名の平均が1.3勝（うち11名が0勝）、
公式のベスト8に8勝の人が出る。勝ち上がりの木が当たっていないため。
1回戦は隣り合う枠どうしの対戦と決まっているので、構造を推測せずに判定できる。
公式の上位8名のうち68〜95%で「1回戦を勝った」と判定でき、外れるのは不戦勝のぶん。
**取りこぼす方向にしか外れないので、誤って勝ちにすることはない。**

効果：「出場だけ」の選手が83%→46%に減り、男子フリーシングルスのBTPは
3種類から18種類に分かれた。

### 管理画面のJSON読み込み

外部で作ったデータを入れる口として「JSON読み込み」を追加した。
読み込んだだけでは保存されないので、内容を確認してから「保存する」を押す。

## 未決事項（依頼者に確認が必要）

- ~~**管理者アカウント。**~~ 決定済み（2026-08-17）：`6x6x6hollow6x6x6@gmail.com`。
  運用を協会に引き継ぐ段になったら `ishikawaboundtennis@gmail.com` を足す（配列なので追記でよい）
- ~~**リポジトリを Public にするか。**~~ 決定済み（2026-08-18）：**Public + GitHub Pages**。
  掲載データはスプレッドシート側にあってリポジトリには入らないので、
  個人情報が増えても Public のままで問題ない
- **制度そのものの合意。** 配点表・集計ルールは提案であって協会の決定事項ではない。
  特に「上位4大会」「カテゴリ跨ぎの算入」、そして
  **交流大会にポイントを付けないという判断そのもの**は要議論。
  「地方大会の常連が一切ランキングに載らない」ことへの反発は出うる
- **団体戦のチームランキングを別立てにするか。** 国民スポーツ大会は団体戦のみなので、
  いまの設計では公式戦なのにどのランキングにも載らない。
  チーム単位で組むなら、チームという概念（選手とは別のマスタ）が要る
- **そもそも「日本ランキング」と名乗るか。** 公式戦が年に数回しかない中で順位を出すことに
  無理があるなら、点数を付けず「公式戦の戦績一覧」として出す選択肢もある。
  看板を掲げると協会内の合意形成が必要になり、そこが一番重い工程になる
- **選手IDの発行と本人紐付け。** 年会費を伴う登録制にするのか、簡易登録にするのか。
  ここで運用負荷が大きく変わる
- **G5（クラブ・サークル主催）の公認基準。** どこまで認めるとランキングの信頼性が保てるか

---

## この案件で作った他のバージョン（参考）

Claude Code に渡す必要はないが、経緯として。

- **青いUI版** … 最初のたたき台。角丸・カード基調
- **新聞版** … NYT風の白黒ブロードシート。却下
- **協会サイト準拠版** … 現行デザインの原型。データはハードコード
- **スプレッドシート直読み版** … `gviz` でCSVを取得する構成。Apps Scriptを使わない分シンプルだが、
  書き込みができないので管理画面が別ファイルになる。今回の構成に置き換えた

現行の構成が最終形。過去版を復活させる必要はない。
